import UnauthorizedHeader from "../components/headers/UnauthorizedHeader.tsx";

const UnauthorizedLayout = () => {
    return (
        <div>
            <UnauthorizedHeader/>
        </div>
    );
};

export default UnauthorizedLayout;