import {useParams} from "react-router-dom";


const TagFinder = () => {
    const {tag} = useParams();
    console.log(tag)
    return (

        <div>
            123'dwe
            dw
            fwevf
            e
        </div>
    );
};

export default TagFinder;